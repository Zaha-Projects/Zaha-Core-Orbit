@extends('layouts.app')
@section('content')
<div class="container py-4"><nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('events.ramadan.iftars.index') }}">{{ __('ramadan_iftars.navigation.title') }}</a></li><li class="breadcrumb-item"><a href="{{ route('events.ramadan.iftars.show',$ramadanIftar) }}">{{ __('ramadan_iftars.navigation.workspace',['id'=>$ramadanIftar->id]) }}</a></li><li class="breadcrumb-item active" aria-current="page">{{ __('ramadan_iftars.titles.monitoring') }}</li></ol></nav><div class="d-flex justify-content-between"><h1 class="h3">{{ __('ramadan_iftars.titles.monitoring') }} — {{ $ramadanIftar->title }}</h1><a href="{{ route('events.ramadan.iftars.show', $ramadanIftar) }}">{{ __('ramadan_iftars.actions.back') }}</a></div>
    <div class="card my-3"><div class="card-header">{{ __('ramadan_iftars.sections.reports') }}</div><ul class="list-group list-group-flush">@forelse($ramadanIftar->monitoringReports as $report)<li class="list-group-item"><a href="{{ route('events.ramadan.iftars.monitoring.edit', [$ramadanIftar,$report]) }}">{{ (app()->getLocale()==='ar' ? optional($report->monitoringMethod)->name_ar : optional($report->monitoringMethod)->name_en) ?: __('ramadan_iftars.business_errors.report_number',['number'=>$report->id]) }}</a> · {{ __('ramadan_iftars.statuses.monitoring.'.$report->status) }} · {{ optional($report->monitor)->name }}</li>@empty<li class="list-group-item text-muted">{{ __('ramadan_iftars.empty.monitoring') }}</li>@endforelse</ul></div>
    @if($monitoringWritable)<h2 class="h5">{{ __('ramadan_iftars.actions.create_report') }}</h2>@include('pages.events.ramadan.monitoring._form', ['formAction'=>route('events.ramadan.iftars.monitoring.store',$ramadanIftar),'formMethod'=>'POST'])@else<div class="alert alert-secondary">{{ __('ramadan_iftars.hints.read_only_monitoring') }}</div>@endif
</div>
@endsection
