# TODO تنفيذ نظام Zaha OPS

> قائمة عمل تشغيلية لتتبع ما تم إنجازه وما تبقى، محدثة بعد آخر تنفيذ.

## ✅ تم إنجازه

## 1) حوكمة وتحليل
- [x] إعداد وثيقة خطة شاملة للتطوير المرحلي.
- [x] إعداد وثيقة تحليل تضارب الأدوار وتحديد قواعد SoD.

## 2) وحدة أجندة زها (Core)
- [x] ترقية هيكل الأجندة بإضافة:
  - `event_categories`
  - `agenda_participations`
  - حقول `event_date`, `event_day`, `department_id`, `event_category_id`, `event_type`, `plan_type`.
- [x] إضافة نماذج `EventCategory` و `AgendaParticipation`.
- [x] تطوير شاشات الأجندة (إنشاء/تعديل/عرض/اعتمادات) بما يطابق المتطلبات الأساسية.
- [x] تنفيذ اعتماد ثنائي: علاقات ثم تنفيذي.
- [x] إضافة أدوار `executive_manager` و `followup_officer` في Seeder.

## 3) الربط مع الخطة الشهرية
- [x] إضافة ميزة مزامنة من الأجندة إلى الخطة الشهرية حسب:
  - الفرع
  - المركز
  - الشهر/السنة
- [x] قواعد المزامنة:
  - إلزامي: يتم جلبه للفروع.
  - اختياري: يتم جلبه إذا كانت مشاركة الفرع = participant.
  - عدم تكرار نفس الفعالية لنفس الفرع.

---

## 🟨 متبقي (أولوية عالية)

## 4) وحدة أجندة زها (استكمال)
- [x] تقييد تعديل الحقول حسب الدور (Field-level authorization) لحقول الأجندة الحساسة ومشاركات الجهات.
- [x] فلترة أصناف الفعالية تلقائياً حسب القسم المعني (UI + server validation).
- [x] دعم أعمدة الجهات المركزية (المشاغل/الاتصال/برامج خلدا/فعاليات خلدا) كحقول مشاركة مستقلة.
- [x] إضافة سجل تدقيق لتعديلات مشاركات الجهات الحساسة في الأجندة.

## 5) الخطة الشهرية للفروع (Form 2)
- [x] نقل الرعاة والشركاء إلى جداول منفصلة بعلاقات مفتوحة العدد.
- [x] إضافة جدول لوج Workflow لتسجيل الإنشاء/التعديل/الاعتماد/الرفض.
- [ ] استكمال جميع حقول النموذج الكبير (الرعاة، الشركاء، المستلزمات، فريق العمل، التقييم...) (تم تنفيذ جزء كبير: رعاة/شركاء/فئة مستهدفة/تعديل الموعد/مخاطبات/رضى الجمهور/تقييم).
- [x] مسار اعتماد كامل متعدد المراحل حسب المطلوب (علاقات ضابط/مدير → برامج ضابط/مدير → تنفيذي).
- [x] تطبيق قاعدة القفل قبل الموعد بـ 5 أيام (قابلة للتعديل من الإعدادات).
- [x] إضافة سجل التعديلات (من عدل ماذا ولمن).

## 6) النقل والحركة (Form 3)
- [x] جداول طلبات الحركة الجديدة (`transport_requests`, `transport_request_trips`, `transport_request_feedback`).
- [x] شاشة قبول/رفض/تعديل من مأمور الحركة.
- [x] تقييم مقدم الطلب بعد إغلاق الحركة.

## 7) الصيانة (Form 4)
- [ ] توسيع نموذج الصيانة ليدعم مسارات (رئيس الفرع/الصيانة/الحاسوب) كلٌ في خانته.
- [ ] توثيق مدد الصيانة والموارد وجهة الدعم وتحليل السبب لكل جهة.
- [ ] اعتماد/إغلاق واضح مع سجل تتبع.

## 8) التقارير وKPIs
- [ ] إنشاء جداول KPI الشهرية.
- [ ] تقارير: داخل الخطة/خارجها، نسبة التعديلات، التزام الخطة، كفاءة الحشد، تقييم الفرع الشهري.
- [ ] إدخال تقييم الالتزام من `followup_officer`.

---

## 🟦 المتطلبات الإدارية قبل الإطلاق
- [ ] اعتماد القائمة النهائية للفروع والأقسام.
- [ ] اعتماد أوزان التقييم النهائية (حالياً 40% رضا + 60% التزام).
- [ ] اعتماد آلية جمع رضا الجمهور (داخلي/خارجي).
- [ ] استكمال تفاصيل الفورم الخامس.

---

## خطة التنفيذ القادمة (Sprint التالي)
1. إكمال Field-level RBAC في الأجندة.
2. استكمال الخطة الشهرية (Form 2) مع الاعتمادات والقفل.
3. ✅ تم بدء تنفيذ Form 3 (النقل) بقاعدة بيانات جديدة + واجهات أساسية (طلبات + معالجة + تقييم).
