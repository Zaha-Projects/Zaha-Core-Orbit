# متتبع ترحيل واجهات النظام (Arabic-First)

> الهدف: توثيق حالة اعتماد الواجهة الحالية بشكل نهائي بعد إغلاق آخر صفحات Legacy.

## تعريف الحالات
- ✅ **معتمدة**: الصفحة تعمل على القالب الموحد `layouts.app` أو قالب المصادقة `layouts.auth`.
- 🔁 **تحسين لاحق**: الصفحة معتمدة وظيفيًا وتحتاج فقط تجويد بصري اختياري.

---

## الحالة الحالية (تحديث موثّق: 2026-04-26)

### الملخص الرقمي النهائي
- الصفحات العاملة على القالب الموحد `layouts.app`: **71 صفحة**.
- الصفحات المتبقية على القوالب القديمة (dashboard aliases السابقة): **0 صفحة**.
- صفحات المصادقة على `layouts.auth`: **3 صفحات** (`welcome`, `auth/login`, `auth/register`).

---

## ✅ ما تم اعتماده بالكامل

### الوحدات الرئيسية
- Dashboard الرئيسي.
- Access.
- Agenda.
- Monthly Activities.
- Finance.
- Maintenance.
- Transport.
- Reports + Enterprise.

### صفحات الأدوار القديمة (Legacy) — تم إغلاقها
- جميع صفحات `resources/views/roles/*` أصبحت تعمل على القالب الموحد، بما فيها صفحات:
  - dashboards حسب الدور.
  - `staff/agenda` و `staff/activities`.
  - `transport/requests/index`.

---

## 🔁 تحسينات لاحقة (لا تمنع الإطلاق)
1. تجويد بصري موحّد لبعض `partials` المشتركة.
2. تدقيق نهائي لتناسق المسافات وأنماط الجداول والنماذج بين الصفحات القديمة المهاجرة والصفحات الجديدة.

---

## قاعدة التحديث في كل PR
- أي تغيير على الواجهة يجب أن يحدّث هذا الملف مباشرةً (الأرقام + الوحدات + نطاق التحسينات اللاحقة).
