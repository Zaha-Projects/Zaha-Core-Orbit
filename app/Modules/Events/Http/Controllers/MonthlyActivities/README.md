# Monthly Activities controllers

Monthly route ownership is grouped here by use case. During the Phase 2.1
compatibility cutover these controllers inherit the unchanged production methods
and helpers from the legacy web controllers. See
`docs/monthly-activities-controller-refactor.md` for the action map and the exact
remaining retirement gate.
