# Monthly Activities controllers

Monthly route ownership and physical endpoint implementations are grouped here by
use case. Shared protected behavior lives in narrowly scoped traits under the
`Concerns` directory; no public route action is defined there. See
`docs/monthly-activities-controller-refactor.md`
for the final action and helper ownership maps.
